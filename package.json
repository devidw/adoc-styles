{
  "name": "monorepo",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "bumpp package.json packages/*/package.json"
  },
  "devDependencies": {
    "bumpp": "^8.2.1"
  }
}